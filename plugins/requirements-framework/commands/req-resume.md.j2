---
name: req-resume
description: "Resume the framework's blocking gates for this session (undo /req-pause)"
argument-hint: ""
allowed-tools: ["Bash"]
git_hash: a165daf
---

> **Workflow position**: escape-hatch (session-scoped). Re-enables the BLOCKING gates that `/req-pause` suppressed for the current session by clearing the pause marker.

# Resume the Requirements Framework (this session)

Clear the session-scoped pause marker via the CLI primitive:

```bash
req resume
```

If `req` is not on PATH, fall back to `python3 hooks/requirements-cli.py resume`.

Then confirm to the user that blocking gates are active again for this session: edits/commits are gated as normal and the end-of-turn Stop check is back on. If the session was never paused, this is a harmless no-op.
