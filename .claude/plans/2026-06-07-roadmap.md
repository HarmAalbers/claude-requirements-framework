# Requirements Framework — Pointwise Roadmap (2026-06-07)

## 0. Where we are
- V3 platform + workflow simplification are **merged to master** (plugin **4.15.4**), CI-gated (`tests/` + render + doctor/verify) and self-publishing.
- **Built & done:** simplification Steps 01–07; V3 Steps 08–18c (schemas, Langfuse observability, Qdrant retrieval, Ragas eval, Jinja2 templates, budget tracker 17a, supervisor, multi-worker fan-out, `/v3-review`); workflow re-sequencing; configurable `workflow:`; brainstorm-on-`UserPromptSubmit`; config-driven supervisor; plugin self-contained; `req doctor`/`verify` plugin-aware; CI hardening.
- **End-state vision:** observable, evaluable, retrieval-augmented LLM platform — `SessionStart` retrieval → `/req` supervisor → parallel workers → aggregator → eval → `SessionEnd` embed+flush.
- **Remaining = three tracks:** finish the 3 open build steps (A), make V3 production-grade (B), adopt it live incrementally (C).

---

## Track A — Finish the V3 build (3 open steps)

- **A1 · Step 17b — pre-run budget guard + per-worker cap** for `/v3-review`. 🔵 **SPECCED 2026-06-07** → `2026-06-07-step-17b-respec.md` (awaiting build approval). Re-specced after live R4 made the original plan stale (V3 is standalone, not `Task`/PreToolUse; the drop-slots lever is inert). Locked: pre-run cost estimate vs the 17a monthly pool + **warn-confirm**, plus a configurable per-worker `max_turns` (the SDK knob that threw `error_max_turns`). No auto-degradation (that's A2-adjacent). *Why:* the **June-15 Agent-SDK credit pool** ($100/mo Max-5x) is ~8 days out; the live run cost **$6.84** with no cap. *Deps:* 17a ✅. *Effort:* ~1d. *Time-pressure: HIGH.*
- **A2 · Step 20 — pin `model: sonnet` on review workers.** Cuts $2–3/run; workers don't need Opus. *Deps:* Step 15 eval ✅ (pinning without eval = gambling); ideally also live data from C1. *Effort:* S.
- **A3 · Step 19 — extract dialect plugin (Dutch / .NET auditors).** Independent, no urgency. *Deps:* none. *Effort:* M.

## Track B — Make V3 production-grade (Task #4)

- **B1 · Portability.** Make `/v3-review` + `hooks/lib/llm` run cleanly outside this repo's exact layout (the hyphen-dir dotted-import constraint, `infra/.env` discovery, the foreground-interactive-only run constraint). *Effort:* M.
- **B2 · Eval-score wiring.** Surface the Ragas score end-to-end: real `trace_id` propagation, `post_to_langfuse` against the live trace (not `case.id`). *Deps:* B1, R5. *Effort:* S–M.
- **B3 · Budget gate.** Wire the 17a/17b ledger as a gate that warns/blocks before an over-budget run. *Couples with A1.* *Effort:* S.

## Track C — Adopt live, incrementally (re-enablement ladder)
*Features are built + on master; the ladder turns them on and validates them live, project-scoped, one rung at a time.* (R1 ✅, R3 ✅.)

- **C1 · R4 — `/v3-review` LIVE.** ✅ **GRADUATED 2026-06-07** — first live run end-to-end (9/10 workers, READY, **$6.84**, traces exported, session `861c5f9e`). Run via `!` foreground — NEVER the Bash tool. Fed A1/A2 the cost + `error_max_turns` data. See `.claude/re-enablement-ledger.md`.
- **C2 · R5 — Observability / Langfuse.** Two layers: **(a)** `/v3-review` traces — ✅ proven live in R4 (OpenInference at the Agent-SDK boundary). **(b)** *full Claude Code* observability (your interactive sessions, not just reviews) — 🟡 per the **2026-06-07 deep-research** (`2026-06-07-claude-code-max-langfuse-observability-research.md`, 101-agent study): use Langfuse's **official Stop-hook integration** (a `Stop` hook reads the local transcript → Langfuse SDK; **Max-OAuth-safe, zero ToS risk, full prompt/completion/tool content**). **Avoid:** OTEL-metrics→Langfuse (traces-only, captures nothing), proxy interception (ToS-risky, Feb–Apr 2026 crackdown), direct Agent-SDK OAuth (ToS violation). Optional 2nd layer: native OTEL **beta** traces for token/cost/latency (fragile). *Effort:* S — drop the official `langfuse_hook.py` Stop hook + `infra/.env` creds + `TRACE_TO_LANGFUSE=true`; raise `CC_LANGFUSE_MAX_CHARS`.
- **C3 · R6 — Eval harness cadence + golden-set expansion.** Wire `run_eval` to a cadence; grow the 5 synthetic cases with mined real bugs (`git log --grep=fix`). *Effort:* M.
- **C4 · R7 — Retrieval / memory / Qdrant enablement.** Turn on `hooks.retrieval.enabled` + qdrant; validate `SessionStart` injection + `SessionEnd` embed live. *Effort:* M.
- **C5 · R2 — status injection + statusline.** Lighter; overlaps simplification Step 03. *Effort:* S.
- **C6 · aux — Obsidian logging + session-learning.** Low-stakes adoption. *Effort:* S.

---

## Recommended critical path
1. **B1 + B3 + A1** — make V3 *safe to run live* (portable + budget-gated + per-call caps). Clears the June-15 cost risk. *(highest priority, time-boxed)*
2. **C1 (R4 live, ~$20–30)** — first real validation, now protected by the gate; harvest eval + cost data.
3. **A2 (Step 20 sonnet)** — use R4 + eval data to justify the model downgrade → cheaper steady-state.
4. **C2 → C3 → C4 (R5 → R6 → R7)** — incremental adoption of observability, eval cadence, retrieval.
5. **A3 (Step 19), C5, C6** — anytime / low priority.

## Open decisions (for Harm)
- Confirm the critical-path order, or reprioritize.
- **17b (A1)** is specced + started (leads on the June-15 time-pressure). **A2 (sonnet)** now has the R4 cost data it needed.
- Anything to *drop* rather than do (e.g. Step 19 if no Dutch/.NET need)?

## Reference docs & decisions
- `2026-06-07-step-17b-respec.md` — **Step 17b spec** (pre-run guard + per-worker cap; re-specced from live data). *Awaiting build approval.*
- `2026-06-07-claude-code-max-langfuse-observability-research.md` — **R5 observability research** (101-agent study). Decision: full-CC observability = Langfuse **official Stop-hook integration**; avoid OTEL-metrics / proxy / direct-SDK-OAuth. Also corroborates the June-15 Agent-SDK-credit policy (→ 17b urgency).
- `2026-06-07-phase3-merge-plan.md` — the "merge what earns it" decision (executed; 185 patches → master).
- `.claude/re-enablement-ledger.md` — rung-by-rung graduation log (R1/R3/R4 ✅, R5 🟡).
