# Example: Minimal Project Configuration
#
# The simplest configuration to get started with the Requirements Framework.
# Place at: .claude/requirements.yaml

version: "1.0"
enabled: true

requirements:
  commit_plan:
    enabled: true
    scope: session
    message: |
      📋 **No commit plan found**

      Create a plan using EnterPlanMode before making changes.

      **To satisfy**: `req satisfy commit_plan`
